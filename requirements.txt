Flask==3.0.3
pytest==8.3.2
faster-whisper==1.0.3
openai==1.40.0
anthropic==0.34.0
pywebview==6.2.1

# Build-time only (PyInstaller packaging) — not required to run the app from source.
pyinstaller==6.10.0
